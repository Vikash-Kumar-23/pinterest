'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

class Yuid {
  static *generator() {
    let serial = 0;
    let random = '';
    let time = '';

    while (true) {
      serial++;
      serial = serial % Number.MAX_SAFE_INTEGER;
      random = Math.floor(Math.random() * Number.MAX_SAFE_INTEGER).toString(36);
      time = new Date().getTime().toString(36);
      yield `${serial.toString(36)}-${time}-${random}`;
    }
  }

  static getId() {
    return this.generator().next().value;
  }

}

exports.Yuid = Yuid;
exports.default = Yuid;
//# sourceMappingURL=yuid.cjs.development.js.map
